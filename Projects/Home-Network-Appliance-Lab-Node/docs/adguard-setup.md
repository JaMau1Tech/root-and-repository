# AdGuard Home Setup

**Status:** ⏳ Planned — Not Started

This document will cover the deployment and configuration of AdGuard Home as MintJammer's household DNS-filtering service.

---

## Planned Scope

- Install Docker (if not already present)
- Deploy AdGuard Home via Docker container
- Configure a static IP reservation for MintJammer on the home router/DHCP server
- Complete AdGuard Home's initial setup wizard (admin credentials, listening interfaces, upstream DNS resolvers)
- Point home network devices (router-level or per-device) to MintJammer as the DNS resolver
- Configure filter lists / blocklists
- Verify filtering is functioning (test domain resolution against a known blocked entry)
- Document any issues encountered and how they were resolved

## Notes

This section is a placeholder outline only. It will be filled in with actual commands, configuration details, and verification steps once the work is performed — consistent with this repository's standard of not documenting steps or results before they've actually been completed.
